# Checklist

## 阶段一：项目初始化与基础框架
- [ ] Vite 项目可启动，`npm run dev` 无报错
- [ ] Tailwind CSS 配色变量正确，暗色模式 class 切换生效
- [ ] 所有 6 条路由（`/`、`/stats`、`/todo`、`/pomodoro`、`/notes`、`/settings`）可访问
- [ ] 侧边栏 64px 宽，5 个导航按钮 + 主题切换 + 设置按钮齐全
- [ ] 侧边栏当前页面对应按钮高亮（紫色填充 + 白色图标）
- [ ] 暗色模式手动切换正常，刷新后保持偏好
- [ ] 暗色模式自动跟随系统正常（未手动设置时）

## 阶段二：Supabase 集成
- [x] Supabase 数据库表（todos、pomodoro_records、notes、user_settings）已创建
- [x] RLS 策略已启用，数据按 user_id 隔离
- [x] 邮箱注册流程正常（注册 → email确认提示 → 登录）
- [x] 登录后访问受保护页面，未登录重定向到 `/auth`
- [x] 登出后无法访问受保护页面
- [x] Supabase 客户端初始化文件 `lib/supabase.ts` 可用

## 阶段三：数据层
- [x] TypeScript 类型定义完整（Todo、PomodoroRecord、Note）
- [x] `useTodoStore` 从 Supabase 加载和写入数据正确
- [x] `usePomodoroStore` 从 Supabase 加载和写入数据正确
- [x] `useNotesStore` 从 Supabase 加载和写入数据正确
- [x] 页面 mount 时自动从 Supabase 加载数据

## 阶段四：待办事项页
- [x] 筛选栏顺序正确：今天 > 全部 > 计划中 > 已完成 > 自定义分类
- [x] "全部""计划中""已完成"为只读视图，不显示输入框
- [x] 自定义分类可新建，出现在末尾
- [x] 自定义分类可重命名（双击编辑，Enter 确认，Escape 取消）
- [x] 自定义分类可删除（确认对话框，同步删除所有关联待办）
- [x] 预计番茄数点击弹出数字滚轮列表（0-99），默认值为 0
- [x] 数字滚轮隐藏滚动条但支持鼠标滚轮，点击外部关闭
- [x] "今天"分类可添加任务，次日自动清空
- [x] "今天"底部日历按钮：日期颜色 无色/绿色/红色 正确
- [x] 日历月份选择器：点击标题弹出年选择器 + 月网格（4×3），有待办月份显示小圆点
- [x] 日历可点击历史日期查看当天待办
- [x] 查看历史日期时输入框隐藏（不允许在历史日期添加任务）
- [x] 任务复选框点击 → 标题划线动画 + 状态变更
- [x] 优先级标签点击 → 高/中/低 轮转
- [x] 不关联番茄的任务不显示番茄数字
- [x] 关联番茄的任务显示 已完成/预计 番茄数
- [x] ▶🍅 按钮跳转 `/pomodoro` 并携带 taskId
- [x] 绑定倒计时的待办显示 🕐 时钟图标（主色调常显），点击跳转仪表盘
- [x] 删除待办弹出确认弹窗，绑定倒计时时特殊提示"该待办已绑定倒计时，删除后将同时移除倒计时"
- [x] 空列表显示引导文案
- [x] 添加动画、删除动画正常

## 阶段五：番茄钟页
- [x] 环形进度条从 12 点钟方向顺时针收缩
- [x] 中央 MM:SS 倒计时数字正确
- [x] 开始/暂停按钮功能正常，图标切换正确
- [x] 暂停后再继续计时准确
- [x] 跳过按钮记录 interrupted 状态
- [x] 重置按钮回到初始时长
- [x] 模式切换重置计时器为对应时长
- [x] 计时归零 → 音效 + 通知 + 环闪动效
- [x] 计时完成自动记录 PomodoroRecord
- [x] 关联任务 completedPomos +1 正确
- [x] 完成 4 个 work 自动建议长休
- [x] TaskLinkCard 显示关联任务信息，无关联时显示"自由专注"
- [x] TaskLinkCard 点击展开可选任务列表，点击外部关闭下拉
- [x] 今日统计显示正确（🍅 图标数 + 总时长）
- [x] 浏览器后台标签页计时精度正常（Date.now() 差值补偿）
- [x] 音效和通知可在设置中开关

## 阶段六：笔记页
- [x] 左侧 300px 笔记列表显示所有笔记
- [x] 搜索框实时过滤正确
- [x] 新建笔记按钮生效，编辑器切换到空笔记
- [x] @uiw/react-md-editor 编辑器正常加载
- [x] 标题可编辑（24px）
- [x] 标签栏可添加/删除标签胶囊
- [x] 自动保存 debounce 1s 后写入 Supabase
- [x] 删除笔记弹出确认弹窗"确定要删除笔记吗？删除后不可恢复"
- [x] 搜索无结果显示"没有找到匹配的笔记"
- [x] 无笔记时显示"还没有笔记，写点什么吧！"

## 阶段七：仪表盘页
- [x] 6 张卡片 Bento Grid 布局（2 列 3 行）
- [x] 问候卡片根据时段显示正确（早安/下午好/晚上好）
- [x] 问候标题可点击编辑（自定义标题，持久化到 Supabase + localStorage）
- [x] 目标倒计时模块（Card 4）：可添加多个待办倒计时方块
- [x] 倒计时方块颜色根据优先级和剩余天数变化，过去日期显示负数并变灰
- [x] 已完成待办的倒计时方块右下角显示绿色 ✓ 图标
- [x] 仅左上角拖拽手柄可拖动排序，点击方块跳转到 `/todo` 对应日期
- [x] 移除倒计时弹出确认弹窗
- [x] 添加倒计时时自动刷新待办列表
- [x] 倒计时数据持久化到 Supabase `user_settings.countdown_todo_ids`
- [x] 今日目标卡片显示番茄目标数
- [x] 待办概览卡片：完成数/总数 + 进度条正确，支持分类筛选
- [x] 最近笔记显示最近 3 条
- [x] 快捷入口 3 个按钮跳转正确
- [x] 无数据时显示引导文案

## 阶段八：统计页
- [x] 粒度切换（日/周/月/年）按钮高亮正确
- [x] 时间范围筛选：日历按钮 → 弹出选择器（自适应日/周/月/年视图）
- [x] 日历选择器：日→日期网格、周→按行选周、月→4×3月网格、年→3×3年网格
- [x] 选中时间范围后所有统计图表重新计算
- [x] 选中历史时间后显示"返回今天"按钮
- [x] 4 个概览卡片数据随粒度和时间范围变化正确
- [x] 趋势柱状图 X 轴按粒度变化（日→24h、周→7天、月→4-5周、年→12月）
- [x] 趋势图支持 duration/count 模式切换
- [x] 任务分布：左侧列表 + 右侧饼图，Top 5 + 其他，随筛选范围变化
- [x] 空数据时显示"暂无专注记录"等提示

## 阶段九：设置页与收尾
- [x] 每日番茄目标数可修改并即时生效
- [x] 工作时长/短休/长休时长可修改并即时生效
- [x] 长休触发间隔可修改并即时生效
- [x] 仪表盘设置：可选择哪些分类显示在待办概览中（持久化 localStorage）
- [x] 仪表盘设置：今天分类子筛选（今天/未来/过去的待办）
- [x] 桌面通知开关可切换
- [x] 音效提醒开关可切换
- [x] 退出登录按钮：清除认证 → 跳转 /auth
- [x] 设置保存到 Supabase，刷新后保持
- [x] 番茄钟 ↔ 待办联动端到端正常（Todo 启动 → 番茄完成 → 计数更新）
